version: '2'
services:
  healthcheck:
    labels:
      io.rancher.container.create_agent: 'true'
      io.rancher.scheduler.global: 'true'
      io.rancher.scheduler.affinity:host_label_ne: io.rancher.host.healthcheck.skip=true
      io.rancher.container.dns: true
    image: llparse/healthcheck:dev
    network_mode: host
    command: healthcheck -metadata-address=10.41.41.41:41
    logging:
      driver: json-file
      options:
        max-size: 25m
        max-file: '2'
