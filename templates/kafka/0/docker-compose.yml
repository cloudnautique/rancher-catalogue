zk:
  tty: true
  image: rawmind/rancher-zk:0.0.1-1
  volumes:
  - /opt/zookeeper/data
  environment:
  - JVMFLAGS=-Xmx${zk_mem}m -Xms${zk_mem}m
  - CONFD_INTERVAL=${zk_interval}
broker:
  tty: true
  image: rawmind/rancher-kafka:0.0.1-1
  links:
  - 'zk:'
  volumes:
  - /opt/kafka/data
  environment:
  - KAFKA_HEAP_OPTS=-Xmx${kafka_mem}m -Xms${kafka_mem}m
  - CONFD_INTERVAL=${kafka_interval}
